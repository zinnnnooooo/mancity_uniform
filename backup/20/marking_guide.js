/* ============================================================
   marking_guide.js — 마킹 & 패치 가이드 화면 전용 스크립트
   시안에 인터랙션 단서가 없는 요소(선수 리스트/패치 카드)는
   정적 목록으로만 표시하며 별도 기능을 추가하지 않는다.
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // 공통 헬퍼(common.js)가 상단 뒤로가기 / 하단 CTA / 하단 네비게이션의
  // data-nav, data-placeholder 바인딩을 이미 처리하므로
  // 이 화면에서 추가로 구현할 전용 인터랙션은 없다.
});
