# UNI:CITY 모바일 웹앱 퍼블리싱 작업계획

## 0. 프로젝트 개요

- **프로젝트명**: UNI:CITY — 맨체스터 시티 유니폼 전문 이커머스 웹앱
- **작업 범위**: 데스크톱 브라우저 안에 표시되는 **모바일 웹앱 화면**만 제작 (반응형 데스크톱 레이아웃 아님)
- **기술 스택**: HTML / CSS / Vanilla JavaScript (프레임워크 없음)
- **콘텐츠 최대 너비**: 480px, 데스크톱 화면에서는 이 480px 프레임을 뷰포트 중앙에 배치
- **기존 프로젝트 파일**: 확인 결과 없음 → 신규 생성
- **작업 원칙**: 이번 단계는 계획 수립만 진행. 코드/퍼블리싱은 이후 화면별 요청 시 순차 진행.

---

## 1. 기존 프로젝트 구조 확인 결과

- `/home/claude`, `/mnt/user-data/outputs` 확인 결과 기존 UNI:CITY 관련 파일 없음.
- 따라서 아래 파일 구조를 신규로 생성한다. (추후 재작업 시에는 기존 파일 우선 재사용)

```
project/
├── ham_menu.html
├── uni_list.html
├── club.html
├── product_detail.html
├── cart.html
├── css/
│   ├── common.css
│   ├── ham_menu.css
│   ├── uni_list.css
│   ├── club.css
│   ├── product_detail.css
│   └── cart.css
└── js/
    ├── common.js
    ├── ham_menu.js
    ├── uni_list.js
    ├── club.js
    ├── product_detail.js
    └── cart.js
```

> `product_detail.html`, `cart.html` 및 관련 css/js는 4~5단계 작업 대상으로 신규 추가.

### 1-2. 6~8단계 추가 파일 구조

- 6~8단계(`marking_guide.html`, `delivery_info.html`, `authenticity.html`)는 기존 파일 구조에 아래와 같이 추가되며, 기존 1~5단계 파일은 변경하지 않는다.
- 3개 화면 모두 `css/common.css`, `js/common.js`를 공통 재사용하고, 화면별 css/js는 기존 파일과 동일한 명명 규칙을 따른다.

```
project/
├── (기존 1~5단계 파일 유지)
├── marking_guide.html   (신규 · 6단계)
├── delivery_info.html   (신규 · 7단계)
├── authenticity.html    (신규 · 8단계)
├── css/
│   ├── (기존 css 유지)
│   ├── marking_guide.css   (신규)
│   ├── delivery_info.css   (신규)
│   └── authenticity.css    (신규)
└── js/
    ├── (기존 js 유지)
    ├── marking_guide.js   (신규)
    ├── delivery_info.js   (신규)
    └── authenticity.js    (신규)
```


### 1-3. 9단계 추가 파일 구조

- 마이페이지는 기존 1~8단계 파일을 변경하지 않고 아래 파일만 신규 추가한다.
- `css/common.css`, `js/common.js`는 기존 공통 파일을 재사용한다.

```text
project/
├── mypage.html
├── css/
│   └── mypage.css
└── js/
    └── mypage.js
```

---

## 2. 첨부 이미지 3장 분석 (모바일 UI 기준)

### 2-1. 햄버거 메뉴 (`ham_menu.html` 대상 — 이미지 3)
- 다크 네이비 배경의 오른쪽(또는 전체폭) 슬라이드 패널 형태
- 상단: `UNI:CITY` 로고 + 닫기(X) 버튼, 하단 보더 라인으로 구분
- 섹션 구조 (섹션명은 회색 소문자 라벨, 자간 넓은 스타일):
  1. **MANCITY** — 맨시티 알아보기
  2. **SHOP** — 전체 유니폼 / 신작 컬렉션 / 베스트셀러 / 레트로 / 스페셜 킷
  3. **SERVICE** — 마킹 & 패치 가이드 / 정품 인증 안내 / 배송 안내
  4. **MY** — 장바구니 / 주문 조회 / 마이페이지
- 각 메뉴 항목: 좌측 텍스트(화이트, 굵은 서체) + 우측 화살표(→) 아이콘, 항목 간 여백 넓음
- 섹션 간 구분선(subtle divider) 존재

### 2-2. 유니폼 리스트 (`uni_list.html` 대상 — 이미지 2)
- 상단 히어로 영역: 에티하드 스타디움 배경 위 선수 이미지, 상단바(뒤로가기 없음/검색 아이콘 우측), 중앙 `CITY STORE` 로고
- 히어로 하단: `26/27 SEASON` 타이틀 + 서브카피
- **2열 그리드 상품 카드** (신상품 섹션):
  - 카드 = 상품 이미지(다크 그라디언트 배경) + `BEST`/`NEW` 뱃지(좌상단) + 시즌/타입명 + 가격(옐로우 포인트 `#DFFF00` 강조)
- **시즌 선택 탭**: 가로 스크롤 pill 버튼 (`26/27`, `25/26`, `24/25`, `23/24` 등), 선택된 탭은 화이트 배경 + 다크 텍스트
- **유니폼 선택 탭**: 전체 / 홈 / 어웨이 / 서드 / 에디션 (동일한 pill 스타일)
- 하단 2열 상품 그리드 반복 (BEST/NEW 뱃지 포함)
- **SPECIAL & HERITAGE** 배너 섹션: 풀블리드 이미지 + 좌하단 텍스트 오버레이 + EXPLORE 링크
- **인기 유니폼** 섹션: 가로 스크롤 카드 리스트
- 하단 푸터: `CITY STORE` 로고 + Official Merchandise Store + 약관/개인정보처리방침/고객센터 링크 + 카피라이트

### 2-3. 구단 소개 (`club.html` 대상 — 이미지 1)
- 상단바: 뒤로가기(←) / `UNI:CITY` 로고 / 공유 아이콘
- **히어로 섹션**: 에티하드 스타디움 야경 풀블리드 이미지, 하단 그라디언트 오버레이 위 `THIS IS OUR CITY.` 타이틀(`OUR CITY`는 스카이블루 강조), 서브카피
- **CLUB IDENTITY** 섹션: 라벨 + `Manchester Is Blue` 타이틀(Blue는 스카이블루) + `맨체스터의 자부심` 서브 + 본문 텍스트 2단락
- **OUR HERITAGE (구단 주요 역사)** — **세로 타임라인**:
  - 좌측에 곡선형 연결 라인(스카이블루), 각 연도 노드가 지그재그(좌→우→좌...) 배치
  - 연도(대형 옐로우/화이트 강조 숫자: 1880, 1937, 1968, 2012, 2023) + 설명 텍스트 + 관련 흑백/컬러 사진
  - 연도별: 1880(창단), 1937(첫 리그 우승), 1968(첫 전성기), 2012(44년만의 PL 우승), 2023(첫 트레블)
- **OUR HONOURS (주요 우승 기록)**: 다크 네이비 배경 위 트로피 아이콘 6개 가로 나열 + 각 하단 라벨
- **OUR CREST (구단 로고 변천사)**: 4개 엠블럼 원형 아이콘 가로 나열 + 하단 연도 구간 라벨
- **OUR HOME (에티하드 스타디움)**: 스타디움 항공샷 풀블리드 이미지 2장(주간/야간) + 하단 정보 그리드(준공/완공/수용인원/UEFA등급)
- **CLUB SIGNATURE COLOR**: 스카이블루 풀블리드 컬러 배너 + `SKY BLUE #6CABDD` 타이틀 + 팬 사진
- **하단 CTA**: `Manchester Is Blue` + `WEAR THE HISTORY` 타이틀 + 옐로우 버튼 `유니폼 둘러보러 가기` (uni_list.html로 연결)

### 2-4. 상품 상세 (`product_detail.html` 대상 — 첨부 이미지 2 · 상품 상세.jpg)
- 상단바(히어로 위 오버레이): 뒤로가기(←) / `UNI:CITY` 로고 / 장바구니 아이콘
- **상품 Hero 이미지**: 유니폼 착용 모델 풀블리드 이미지, 배경에 에티하드 스타디움 텍스트 워터마크가 은은하게 비침
- **이미지 슬라이드 인디케이터**: 이미지 하단 가로 세그먼트 바(5개), 첫 번째 세그먼트만 하늘색 활성, 나머지는 다크 비활성
- **정품 인증 배지**: 체크 아이콘 + `정품 인증 상품` 텍스트, 반투명 다크 필(pill) 배지
- **상품명**: `2026/27 시즌 홈 레플리카 유니폼`
- **가격**: `189,000원`, 옐로우(`#DFFF00`) 포인트 대형 타이포
- **사이즈 선택**: `S / M / L / XL / XXL` 5분할 버튼, 영역 우측 상단에 `사이즈 가이드` 텍스트 링크, 선택된 사이즈는 화이트 테두리로 강조(예시 이미지는 `L` 선택 상태)
- **선수 마킹 드롭다운**: 라벨 `선수 마킹` + `No marking` 기본값의 select형 드롭다운(우측 화살표 아이콘)
- **오피셜 패치 드롭다운**: 라벨 `오피셜 패치` + `No patch` 기본값의 select형 드롭다운
- **상품 수량 조절**: 라벨 `제품 수량` + 마이너스/수량/플러스 버튼 (우측 정렬)
- **하단 고정 액션바**: `장바구니`(아웃라인 버튼) + `바로 구매`(옐로우 버튼) 2분할, 화면 하단에 고정(sticky/fixed)

### 2-5. 장바구니 (`cart.html` 대상 — 첨부 이미지 1 · 장바구니.jpg)
- 상단바: 뒤로가기(←) / `UNI:CITY` 로고 / 장바구니 아이콘
- **전체 선택 영역**: 좌측 `전체 선택` 체크박스 + 라벨, 우측 `선택 삭제` 텍스트 버튼(서브톤 배경)
- **장바구니 상품 카드** (반복):
  - 좌측 개별 선택 체크박스
  - 상품 썸네일(정사각형, 다크 그라디언트)
  - 상품명(화이트, 굵은 서체) + 옵션 텍스트(`SIZE L | AGUERO 10 | PL PATCH` 형태, 회색 소문자 캡션)
  - 수량 조절(−/01/+)
  - 가격(옐로우 포인트, 우측 정렬)
  - 카드 배경은 본문보다 약간 밝은 다크 패널(둥근 모서리)
- **결제 예정 금액 패널**:
  - 타이틀 `결제 예정 금액`
  - 행 목록: `상품 금액` / `마킹 추가 비용`(+표시) / `패치 추가 비용`(+표시) / `배송비`(무료 조건 텍스트, 하늘색 링크 톤 — 예: `무료 (20만원 이상 구매 시 무료 배송)`)
  - 구분선 이후 `총 결제 금액` 라벨 + 대형 옐로우 금액(우측 정렬)
- **하단 고정 CTA 버튼**: `총 N개 상품 구매하기` (옐로우, 풀와이드), N과 금액은 선택된 상품 기준으로 자동 계산

### 2-6. 마킹 & 패치 가이드 (`marking_guide.html` 대상 — 첨부 이미지 「마킹_패치_가이드」)
- 상단바: 뒤로가기(←) / 중앙 `CITY STORE` 로고 / 우측 검색 아이콘
- **히어로 섹션**: FODEN 47 유니폼 배경 풀블리드 이미지, `MAKE IT` (화이트) + `YOURS.` (라임 `#DFFF00`) 타이틀, 서브카피 `선수 마킹부터 오피셜 패치까지, 나만의 유니폼을 완성해보세요.`, 하단 화살표(↓) 아이콘
- **PLAYER MARKING 라벨 필(pill)** + 섹션 타이틀 `당신의 영웅을 새기다` + 본문 설명(`좋아하는 선수의 이름과 등번호를 선택해 공식 스타일의 마킹을 추가할 수 있습니다. 프리미어리그 규격의 폰트와 소재가 적용됩니다.`)
- **선수 리스트**: `HAALAND 9` / `FODEN 47` / `RODRI 16` — 좌측 선수명, 우측 등번호 정렬된 리스트형 UI
- 안내 아이콘 + 캡션(`시즌별 유니폼 디자인에 따라 마킹 폰트 및 색상에 차이가 있을 수 있습니다.`)
- **마킹 예시 이미지**: `FODEN 47` 유니폼 등판 클로즈업 이미지 + 이미지 위 `PREMIER LEAGUE FONT` 라벨 필
- **OFFICIAL PATCH 라벨 필** + 섹션 타이틀 `영광의 상징을 더하다` + 본문 설명(`참여하는 대회의 오피셜 패치를 부착하여 유니폼의 가치를 높이세요. 양팔 소매에 규정에 맞게 부착됩니다.`)
- **패치 카드 3종** (세로 반복, 각 카드 = 유니폼 소매 이미지 배경 + 패치 로고 오버레이 + 타이틀 + 설명 + 하단 아이콘 링크):
  1. 프리미어리그 패치 — `우측 소매에 부착되는 프리미어리그 공식 패치입니다. 챔피언 자격일 경우 황금색 챔피언 패치가 제공됩니다.` / 하단 `우측 소매 부착`
  2. 챔피언스리그 패치 세트 — `우측 소매의 챔피언스리그 로고 패치와 좌측 소매의 파운데이션 패치가 세트로 부착됩니다.` / 하단 `양측 소매 부착`
  3. FIFA 클럽 월드컵 패치 — `세계에서 가장 권위있는 대회인 클럽 월드컵패치를 우측 소매에 부착합니다.` / 하단 `우측 소매 부착`
- **주문 전 확인사항 섹션**: 체크 아이콘 + `주문 전 확인사항` 타이틀, 번호 리스트:
  1. 시즌별 디자인 확인 — `마킹 폰트와 패치 디자인은 해당 시즌 규정을 따릅니다. 과거 시즌 유니폼에는 마킹/패치 부착이 불가능할 수 있습니다.`
  2. 커스텀 마킹 오타 확인 — `본인 이름 및 커스텀 마킹 주문 시 영문 스펠링과 등번호를 반드시 재확인해 주세요.`
  3. 경고 강조 항목(노란 배경 카드, 경고 아이콘): `교환/환불 안내` — `마킹 및 패치 작업이 진행된 커스텀 유니폼은 제작 특성상 단순 변심으로 인한 교환/환불이 불가능합니다. 신중한 구매 부탁드립니다.`
- **하단 CTA**: `READY TO WEAR` / `YOUR CITY?` 타이틀 + 라임 버튼 `유니폼 보러가기 →`
- **하단 고정 네비게이션**: 홈 / 장바구니 / 찜 / 마이 4개 아이콘 탭바

### 2-7. 배송 안내 (`delivery_info.html` 대상 — 첨부 이미지 「배송_안내」)
- 상단바: 뒤로가기(←) / 중앙 `CITY STORE` 로고 / 우측 검색 아이콘
- **히어로 섹션**: 접힌 MAN CITY 유니폼 배경 풀블리드 이미지, `FROM CITY` (화이트) + `TO YOUR CITY` (라임) 타이틀, 서브카피 `주문부터 도착까지, 빠르고 안전하게 전달합니다.`
- **DELIVERY PROCESS 라벨 필** + 섹션 타이틀 `배송 프로세스`
- **세로 스텝 리스트(4단계, 좌측 세로 연결선 + 원형 인디케이터)**:
  1. `ORDER` — 주문 완료 및 결제 확인 (첫 단계, 라임 활성 표시)
  2. `PREPARING` — 상품 준비 및 마킹/패치 작업 진행
  3. `SHIPPING` — 상품 포장 및 택배사 인계 (배송 시작)
  4. `DELIVERED` — 고객님께 배송 완료
- **예상 배송 기간 섹션**: 트럭 아이콘 + `예상 배송 기간` 타이틀, 카드 2개:
  1. `일반 상품 (노마킹)` — 대형 강조 텍스트 `2-3일`(하늘색 포인트) + 캡션 `영업일 기준 (주말/공휴일 제외)`
  2. `커스텀 상품 (마킹/패치)` — 대형 강조 텍스트 `+3-5일` + 캡션 `수작업으로 진행되어 추가 작업 기간 발생`
- **안전한 배송 섹션**: 타이틀 `안전한 배송` + 서브 `유니폼의 컨디션까지 안전하게.` + 본문(`모든 상품은 꼼꼼한 검수 과정을 거쳐 출고됩니다. 프리미엄 패키징을 통해 오염이나 손상 없이 최상의 상태로 고객님께 전달하는 것을 최우선으로 합니다.`) + 포장 과정 이미지(패키징 파우치를 손으로 다루는 사진)
- **자주 묻는 질문(FAQ) 섹션**: 물음표 아이콘 + `자주 묻는 질문` 타이틀 + 아코디언 3개(+ 토글 아이콘):
  1. 배송 조회는 어디서 하나요?
  2. 마킹 상품은 배송이 더 오래 걸리나요?
  3. 배송지를 변경할 수 있나요?
- **하단 CTA**: `TRACK YOUR KIT` 타이틀 + 서브카피 `주문한 유니폼의 현재 배송 상태를 확인하세요.` + 라임 버튼 `주문 / 배송 조회 →`
- **하단 고정 네비게이션**: 홈 / 배송조회 / 찜 / 마이 4개 아이콘 탭바

### 2-8. 정품 인증 안내 (`authenticity.html` 대상 — 첨부 이미지 「정품_인증_안내」)
- 상단바: 뒤로가기(←) / 중앙 `정품 인증 안내` 타이틀
- **히어로 섹션**: 엠블럼/유니폼 텍스처 배경 풀블리드 이미지, `100%` (화이트) + `AUTHENTIC.` (라임) 타이틀, 서브카피 `당신이 입는 CITY, 정품이라는 확신까지 함께 제공합니다.`, 하단 화살표(↓) 아이콘
- **인증 배지 섹션**: 원형 체크 배지 아이콘 + `100%` 대형 타이틀 + 캡션 `정품 상품만을 취급합니다.`
- **섹션 타이틀**: `정품은 디테일에서 차이가 납니다.`
- **디테일 카드 4종 (01~04, 세로 반복 · 각 카드 = 대형 번호 + 대문자 타이틀 + 설명, 01/04는 하단에 관련 이미지 포함)**:
  1. `PRODUCT TAG` — 제품 택 및 품번 확인 (+ 제품 택 클로즈업 이미지)
  2. `CLUB CREST` — 구단 엠블럼 마감 및 디테일 확인
  3. `BRAND DETAIL` — 브랜드 로고 및 봉제 상태 확인
  4. `PRODUCT CODE` — 제품 고유 코드 및 상품 정보 확인 (+ 제품 코드 라벨 클로즈업 이미지)
- **검수 프로세스 섹션**: 타이틀 `검수 프로세스` + 세로 스텝 리스트(4단계, 좌측 세로 연결선 + 원형 인디케이터, 첫 단계만 활성 스타일):
  1. `상품 입고` — 공식 채널을 통한 제품 확보
  2. `제품 검수` — 전문 검수팀의 외관 및 디테일 확인
  3. `정품 확인` — 코드 대조 및 최종 승인
  4. `출고` — 고객에게 안전하게 배송
- **하단 CTA**: `WE CHECK.` / `YOU TRUST.` 타이틀 + 서브카피 `유니폼을 고르는 순간부터 받아보는 순간까지, 안심할 수 있도록 확인합니다.` + 라임 버튼 `정품 유니폼 둘러보기 →`


### 2-9. 마이페이지 (`mypage.html` 대상 — 첨부 이미지 「마이페이지」)
- 상단바: 뒤로가기(←) / 중앙 `UNI:CITY` 로고 / 우측 장바구니 아이콘
- **프로필 영역**: 사용자 프로필 이미지 + 아이디 `CTID9320` + 등급/라벨 `맨시티 팬`
- **요약 카드 3개**: 주문 내역 `12` / 배송 중 `1` / 위시리스트 `8`, 3분할 카드 UI
- **최근 주문 내역 카드**:
  - 타이틀 `최근 주문 내역` + 우측 `상세보기 >`
  - 상품 썸네일 + 상태 배지 `상품 준비 중`
  - 상품명 `26/27 홈 레플리카 유니폼`
  - 옵션 정보 `옵션: L / 마킹: FODEN 47`
  - 가격 `₩189,000` 라임 포인트
  - 하단 주문 상태 4단계: `결제완료 → 상품준비 → 배송중 → 배송완료`, 현재 단계 강조
- **메뉴 리스트**: 구매내역 / 배송추적 / 위시리스트 / 개인정보 수정 / 고객센터
  - 각 항목은 좌측 아이콘 + 텍스트 + 우측 화살표
  - 동일한 카드형 리스트 스타일
- **하단 고정 네비게이션**: 홈 / 장바구니 / 위시리스트 / 마이 4개 아이콘, 마이 탭 활성
- 전체 톤은 기존 UNI:CITY의 다크 네이비/블루그레이 배경, 스카이블루 포인트, 라임 가격 강조 스타일 유지

---

## 3. 공통 스타일(`css/common.css`) 정의 계획

### 3-1. 디자인 토큰 (CSS 변수)
```css
--color-bg-navy:      #0B1622 (다크 네이비, 페이지 배경)
--color-bg-navy-2:    #101F30 (카드/패널 배경, 약간 밝은 톤)
--color-sky-blue:     #6CABDD (Manchester City Sky Blue, 메인 포인트)
--color-accent-lime:  #DFFF00 (가격/CTA 강조 포인트)
--color-white:        #FFFFFF
--color-text-muted:   rgba(255,255,255,0.6~0.7) (서브텍스트, 라벨)
--color-divider:      rgba(255,255,255,0.08~0.12)
--max-width: 480px
--font-family: 시스템 산세리프 기반, 헤드라인은 굵고 압축된 느낌(예: 'Pretendard', 'Arial Narrow' 대체 스택), 숫자(연도/가격)는 더 굵고 임팩트 있게
```

### 3-2. 레이아웃 규칙
- `body`: `display:flex; justify-content:center;` 로 데스크톱에서 중앙 정렬, 배경은 네이비보다 더 어두운 톤(모바일 프레임 외부 여백) 또는 단색 처리
- `.app-frame` (또는 각 페이지 최상위 wrapper): `max-width:480px; width:100%; margin:0 auto; min-height:100vh; position:relative; overflow-x:hidden;`
- 데스크톱에서 480px를 넘는 폭에서도 `.app-frame` 내부 요소는 절대 폭 확장하지 않음 (내부는 전부 %/px 기반 모바일 고정 비율 유지)
- 공통 상단바(`.topbar`) 컴포넌트: 좌/중/우 3분할 flex, position sticky 또는 absolute(히어로 위 오버레이용 variant 별도)
- 공통 버튼(pill 탭, CTA 버튼), 공통 뱃지(BEST/NEW), 공통 섹션 타이틀(`OUR ~~~` 대문자 + 국문 서브) 스타일 클래스 정의

### 3-3. 공통 JS(`js/common.js`) 역할
- 페이지 공통 초기화(뷰포트 높이 보정 등 필요 시)
- 화면 간 이동 헬퍼(예: `location.href` 이동 함수)는 필요 최소한만 정의 — 불필요한 기능 추가 금지 원칙 준수

---

## 4. 화면별 퍼블리싱 계획

### 4-1. 햄버거 메뉴 (`ham_menu.html` / `ham_menu.css` / `ham_menu.js`)
- 구조: 헤더(로고+닫기) → 4개 섹션 리스트(MANCITY/SHOP/SERVICE/MY)
- 스타일: 다크 네이비 배경, 섹션 라벨은 소문자 회색 letter-spacing, 메뉴 항목은 flex space-between(텍스트+화살표), 항목 클릭 시 hover/active 시각 피드백
- JS: 닫기(X) 버튼 클릭 시 동작(예: 이전 페이지로 돌아가기 또는 패널 숨김), 각 메뉴 항목 클릭 시 해당 페이지로 이동(전체 유니폼 → uni_list.html, 맨시티 알아보기 → club.html 등 존재하는 페이지로만 연결, 나머지는 placeholder)
- 단독 페이지로도 열람 가능하되, 다른 화면에서 트리거되는 오버레이 패널 형태도 고려해 마크업 재사용 가능하게 설계

### 4-2. 유니폼 리스트 (`uni_list.html` / `uni_list.css` / `uni_list.js`)
- 구조: 상단바(검색 아이콘) → 히어로(스타디움+선수 이미지, 타이틀) → 신상품 2열 그리드 → 시즌 탭 → 유니폼타입 탭 → 상품 2열 그리드(탭에 따라 필터링) → SPECIAL&HERITAGE 배너 → 인기 유니폼 가로 스크롤 → 푸터
- 스타일: 상품 카드 다크 그라디언트, 뱃지(BEST 옐로우/NEW 스카이블루 톤 구분), 가격 텍스트는 옐로우 포인트, pill 탭 active 상태 스타일
- JS: 시즌 탭 / 유니폼 선택 탭 클릭 시 active 클래스 토글(실제 데이터 필터링은 더미 데이터 배열 기반으로 최소 구현), 가로 스크롤 영역 자연스러운 스크롤 지원
- 이미지: 실제 유니폼 사진 에셋이 없으므로 우선 플레이스홀더(단색/그라디언트 블록 또는 텍스트 라벨)로 대체 — 추후 실제 이미지 교체 가능하도록 구조화

### 4-3. 구단 소개 (`club.html` / `club.css` / `club.js`)
- 구조: 상단바(뒤로가기/로고/공유) → 히어로 → CLUB IDENTITY → OUR HERITAGE(타임라인) → OUR HONOURS → OUR CREST → OUR HOME → SIGNATURE COLOR → 하단 CTA(유니폼 둘러보러 가기)
- 스타일 난이도가 가장 높은 화면: 세로 타임라인의 지그재그 배치 + 곡선 연결선(SVG 또는 CSS pseudo-element로 구현 검토), 연도 대형 타이포그래피
- JS: 상단 뒤로가기 버튼 동작, 하단 CTA 버튼 클릭 시 `uni_list.html`로 이동, (선택) 타임라인 스크롤 시 순차 등장 애니메이션은 과도한 기능 추가이므로 기본은 정적 배치로 구현하고 별도 요청 시에만 추가

### 4-4. 상품 상세 (`product_detail.html` / `product_detail.css` / `product_detail.js`)
- 구조: 상단바(오버레이) → 상품 히어로 이미지 + 슬라이드 인디케이터 → 정품 인증 배지 + 상품명/가격 → 사이즈 선택(+사이즈 가이드) → 선수 마킹 드롭다운 → 오피셜 패치 드롭다운 → 상품 수량 조절 → 하단 고정 `장바구니 / 바로 구매` 액션바
- 스타일: 히어로 이미지는 실사 에셋이 없어 우선 그라디언트 + 텍스트 플레이스홀더로 대체(추후 실제 이미지 교체 가능하도록 구조화), 사이즈 버튼은 공통 pill 스타일의 사각형 토글 변형, 드롭다운은 다크 패널 select 스타일로 공통화 검토
- **데이터 구조(공통 JS 데이터 설계)**: 상품 정보를 JS 객체로 정의 (`id, name, thumbnail, basePrice, sizes: [], markingOptions: [{label, fee}], patchOptions: [{label, fee}]`) — 마킹/패치 옵션 변경 시 `basePrice + markingFee + patchFee`로 총 가격 재계산
- JS: 사이즈 버튼 단일 선택 토글, 마킹/패치 드롭다운 `change` 이벤트로 가격 실시간 갱신, 수량 +/− 버튼(최소 1개), `장바구니`/`바로 구매` 클릭 시 현재 선택 옵션(사이즈/마킹/패치/수량/가격)을 하나의 데이터 객체로 구성 — **cart.html과 필드명을 통일**해 향후 실제 연동(로컬 저장 등) 시 그대로 재사용 가능하도록 준비(이번 범위에서는 데이터 객체 구성까지만, 실제 저장/이동 로직은 최소 동작만 구현)
- 상단 뒤로가기 버튼은 `common.js` 헬퍼 재사용

### 4-5. 장바구니 (`cart.html` / `cart.css` / `cart.js`)
- 구조: 상단바 → 전체 선택 / 선택 삭제 → 장바구니 상품 카드 리스트(반복) → 결제 예정 금액 패널 → 하단 고정 `총 N개 상품 구매하기` 버튼
- 스타일: 카드형 리스트(둥근 모서리, 본문보다 밝은 다크 패널), 체크박스 커스텀 스타일(공통 컴포넌트화 검토), 금액 패널은 라벨(좌)/금액(우) 정렬 + 구분선으로 총액 강조
- **데이터 구조**: 더미 장바구니 배열을 JS로 정의 — 각 항목은 `{ id, name, thumbnail, option: { size, marking, patch }, price, markingFee, patchFee, qty, checked }` 형태로, `product_detail.html`에서 넘어올 데이터 필드명과 동일하게 설계하여 향후 실제 연동 가능하도록 준비(이번 범위는 더미 데이터 기반 구현까지)
- JS:
  - 전체 선택 체크박스 ↔ 개별 체크박스 상태 동기화
  - `선택 삭제`: 체크된 항목을 배열에서 제거 후 리스트 재렌더링
  - 수량 +/− 버튼: 최소 1개, 변경 시 해당 상품 가격 및 결제 예정 금액 재계산
  - 체크 상태 기준으로 `상품 금액 / 마킹 추가 비용 / 패치 추가 비용 / 총 결제 금액` 실시간 재계산
  - 하단 CTA 버튼의 `총 N개 상품 구매하기` 텍스트도 선택된 상품 개수에 맞춰 자동 갱신
  - 배송비: 표시 텍스트(`무료 (20만원 이상 구매 시 무료 배송)`)는 시안대로 재현하되, 실제 조건부 계산(상품 금액 합계 기준 20만원 이상 여부)까지 최소 로직으로 구현

### 4-6. 마킹 가이드 (`marking_guide.html` / `marking_guide.css` / `marking_guide.js`)
- 구조: 상단바(로고/검색) → 히어로(MAKE IT YOURS.) → PLAYER MARKING 라벨+타이틀+설명 → 선수 리스트(HAALAND/FODEN/RODRI) → 안내 캡션 → 마킹 예시 이미지 → OFFICIAL PATCH 라벨+타이틀+설명 → 패치 카드 3종(PL/UCL/FIFA) → 주문 전 확인사항(1,2 + 경고 카드) → 하단 CTA(유니폼 보러가기) → 하단 고정 네비게이션
- 스타일: 히어로/패치 카드/마킹 예시 이미지는 실사 에셋이 없어 그라디언트+텍스트 플레이스홀더로 대체(추후 교체 가능하도록 구조화), 선수 리스트는 `uni_list.html`의 pill/리스트 스타일과 톤 통일, 경고 카드만 노란색 계열 배경으로 시각적 구분, 패치 카드는 공통 카드 컴포넌트 스타일 재사용 검토
- JS: 상단 뒤로가기 버튼은 `common.js` 헬퍼 재사용, 하단 CTA `유니폼 보러가기` 클릭 시 `uni_list.html` 이동, 하단 네비게이션 아이콘은 기존에 존재하는 페이지로만 연결(홈 → uni_list.html, 장바구니 → cart.html, 나머지는 placeholder)
- 선수 리스트/패치 카드는 시안 그대로 정적 목록으로 구현(별도 선택/필터 기능 없음 — 시안에 인터랙션 단서 없으므로 추가 기능 구현 금지)

### 4-7. 배송 안내 (`delivery_info.html` / `delivery_info.css` / `delivery_info.js`)
- 구조: 상단바(로고/검색) → 히어로(FROM CITY TO YOUR CITY) → DELIVERY PROCESS 라벨+타이틀 → 세로 스텝 리스트(ORDER→PREPARING→SHIPPING→DELIVERED) → 예상 배송 기간(일반/커스텀 카드 2개) → 안전한 배송(설명+이미지) → 자주 묻는 질문(아코디언 3개) → 하단 CTA(TRACK YOUR KIT) → 하단 고정 네비게이션
- 스타일: 세로 스텝 리스트는 `club.html`의 타임라인 컴포넌트와 유사한 좌측 연결선+원형 인디케이터 구조를 재사용/변형(지그재그 없이 좌측 정렬 단일 라인으로 단순화), 배송기간 카드는 강조 숫자(2-3일/+3-5일)를 큼직한 포인트 타이포로 처리, FAQ는 클릭 시 펼침/접힘되는 아코디언 컴포넌트로 구현
- JS: 상단 뒤로가기 버튼은 `common.js` 헬퍼 재사용, FAQ 아코디언 항목 클릭 시 개별 open/close 토글(다른 항목과 독립적으로 동작), 하단 CTA `주문 / 배송 조회` 클릭 시 이동 대상 페이지가 시안 내 존재하지 않으므로 클릭 시 최소 동작(예: 알림 또는 placeholder)만 구현, 하단 네비게이션은 기존 페이지로만 연결

### 4-8. 정품 인증 안내 (`authenticity.html` / `authenticity.css` / `authenticity.js`)
- 구조: 상단바(뒤로가기/타이틀) → 히어로(100% AUTHENTIC.) → 인증 배지(100%) → 섹션 타이틀 → 디테일 카드 4종(01 PRODUCT TAG~04 PRODUCT CODE, 01/04만 이미지 포함) → 검수 프로세스(세로 스텝 4단계) → 하단 CTA(WE CHECK. YOU TRUST.)
- 스타일: 디테일 카드는 대형 번호(옐로우/화이트 포인트 넘버링) + 카드형 패널 반복 컴포넌트로 구현, 이미지가 있는 카드(01, 04)와 없는 카드(02, 03)를 하나의 카드 컴포넌트 variant로 공통화, 검수 프로세스는 `delivery_info.html`의 세로 스텝 컴포넌트와 톤 통일(공통 CSS 클래스 재사용 검토)
- JS: 상단 뒤로가기 버튼은 `common.js` 헬퍼 재사용, 하단 CTA `정품 유니폼 둘러보기` 클릭 시 `uni_list.html`로 이동
- 이 화면은 하단 고정 네비게이션 탭바가 시안에 존재하지 않으므로 추가하지 않음(시안에 없는 요소 추가 금지 원칙 준수)


### 4-9. 마이페이지 (`mypage.html` / `mypage.css` / `mypage.js`)
- 구조: 상단바 → 프로필 영역 → 주문/배송/위시리스트 요약 카드 3개 → 최근 주문 내역 카드 → 구매내역/배송추적/위시리스트/개인정보 수정/고객센터 메뉴 리스트 → 하단 고정 네비게이션
- 스타일: 첨부 시안의 블루그레이 카드형 UI 유지, 숫자/가격은 강한 타이포로 강조, 최근 주문 상품 가격은 라임(`#DFFF00`) 적용, 주문 진행 상태는 현재 단계만 화이트/스카이블루로 강조
- JS: 메뉴/하단 네비게이션은 존재하는 페이지로만 연결하고 미구현 메뉴는 placeholder 처리, 최근 주문 `상세보기`는 추후 주문 상세 페이지 연결을 고려해 구조만 준비
- 프로필/주문 요약 값은 이후 실제 데이터 연동이 가능하도록 DOM 데이터 영역을 명확히 분리하되, 이번 화면은 시안 재현을 우선해 정적 데이터로 구현

---

## 5. 모바일 화면 크기 대응 계획

- 기준 디자인 폭: 첨부 시안 기준 약 375~390px 뷰포트 대응 (일반적 모바일 폭)
- `.app-frame { max-width: 480px; width: 100%; }` 로 480px 상한 고정
- 데스크톱(뷰포트 ≥480px)에서는 `body`를 flex center로 두어 `.app-frame`이 중앙에 고정폭으로 표시, 좌우 여백은 배경색(예: 더 어두운 네이비 또는 블랙)으로 채움
- **태블릿/데스크톱 전용 브레이크포인트(min-width media query로 레이아웃 변경) 사용 금지** — `.app-frame` 내부는 항상 동일한 모바일 구조 유지
- 384px 미만의 초소형 화면 대응이 필요한 경우에도 구조 변경 없이 내부 패딩/폰트 크기만 최소한으로 유동 처리(선택 사항, 기본은 480px 고정 비율 유지)

---

## 6. 공통 인터랙션 및 페이지 연결 계획

- **페이지 간 이동**:
  - `uni_list.html` 상단 → 필요 시 햄버거 메뉴 오버레이 진입(공통 컴포넌트 재사용 검토)
  - `club.html` 하단 CTA `유니폼 둘러보러 가기` → `uni_list.html` 이동
  - `ham_menu.html` 내 "맨시티 알아보기" → `club.html`, "전체 유니폼" → `uni_list.html`
  - `ham_menu.html` SERVICE 섹션 내 "마킹 & 패치 가이드" → `marking_guide.html`, "정품 인증 안내" → `authenticity.html`, "배송 안내" → `delivery_info.html`
  - `marking_guide.html` / `authenticity.html` 하단 CTA → `uni_list.html` 이동
  - 하단 네비게이션의 마이 탭 → `mypage.html` 이동
  - `mypage.html`의 장바구니/배송추적/위시리스트 메뉴는 해당 페이지가 존재하는 경우 연결하고, 미구현 페이지는 placeholder 처리
- **인터랙션 최소 원칙**: 시안에 없는 기능(장바구니 로직, 실제 검색 기능, 실제 주문 조회 등)은 구현하지 않고 UI만 시안대로 재현. 클릭 시 이동/토글 등 명확히 유추 가능한 기본 동작만 JS로 구현.
- **탭/토글류**: 시즌 탭, 유니폼 선택 탭은 active 클래스 전환 정도의 가벼운 JS로 처리, `delivery_info.html`의 FAQ 아코디언도 동일한 수준의 open/close 토글로 처리

---

## 7. 최종 UI 검수 계획

- 각 화면 완성 시 다음을 체크:
  1. 480px 프레임 내에서 시안과 레이아웃/간격/컬러/타이포그래피가 최대한 일치하는지
  2. 데스크톱 브라우저에서 창을 넓혀도 모바일 프레임 구조가 유지되고 중앙 정렬되는지
  3. 공통 CSS/JS가 화면 간 중복 없이 정상 공유되는지
  4. 불필요한 섹션/기능이 추가되지 않았는지
  5. 페이지 간 연결(햄버거 메뉴 ↔ 유니폼 리스트 ↔ 구단 소개 ↔ 마킹 가이드 ↔ 배송 안내 ↔ 정품 인증 안내 ↔ 마이페이지)이 자연스러운지

---

## 8. 작업 순서 (전체 9단계, 화면별 순차 진행)

1. `css/common.css` + `js/common.js` 기초 골격 작성 (디자인 토큰, 공통 레이아웃) — ✅ 완료
2. `club.html` (구단 소개) — ✅ 완료
3. `ham_menu.html` (햄버거 메뉴) — 예정
4. `uni_list.html` (유니폼 리스트) — 예정
5. `product_detail.html` (상품 상세) / `cart.html` (장바구니) — 예정 (요청 시 순차 진행, 두 화면은 데이터 필드명을 통일해 함께 검토)
6. `marking_guide.html` (마킹 & 패치 가이드) — 예정
7. `delivery_info.html` (배송 안내) — 예정
8. `authenticity.html` (정품 인증 안내) — 예정
9. `mypage.html` (마이페이지) — 예정
10. 전체 통합 후 최종 검수 (섹션 7 체크리스트 기준)

> 본 문서는 계획 단계 산출물이며, 실제 퍼블리싱 코드는 사용자의 화면별 진행 요청 이후 시작합니다. 위 순서는 고정 순번이 아니라 참고용이며, 사용자가 지정하는 화면을 우선 진행합니다.
